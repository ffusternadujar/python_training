while True:
    x = input("Enter the pass: ")
    if x == "open_sesame":
        print("Access Granted")
        break
    if  (x == "hola"):
        continue
    print("Access Denied")


# break vs continue
# break termina el bucle mientras que continue salta a la siguiente iteración del bucle ignorando el código restante en la iteración actual.
